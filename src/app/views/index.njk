{% extends "layout.njk" %}

{% block head %}
  <title>Foodfy - Home</title>
{% endblock head %}

{% block content %}
  <main>
    <div class="banner">
      <div class="article">
        <h1>As melhores receitas</h1>
        <p>
          Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.
        </p>
      </div>
      <img src="/assets/chef.png" alt="Chefe de cozinha" srcset="">
    </div>
    <div class="list">
      <h1>Mais acessadas</h1>
      <div class="cards">
        {% for recipe in recipes %}
          <div class="card" id="{{recipe.index}}">
            <a href="/recipe/{{recipe.id}}">
              <img src="{{recipe.image}}" alt="{{recipe.title}}" srcset="">
              <h2>{{recipe.title}}</h2>
              <p class="autor">por
                {{recipe.author}}</p>
            </a>
          </div>
        {% endfor %}
      </div>
    </div>
  </main>

{% endblock content %}